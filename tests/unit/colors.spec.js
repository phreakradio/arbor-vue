'use strict';

const assert = require('assert');

describe('Colors::CSS', function(){
	it('should correct correlate names to color hex', function(){

	});

});

describe('Colors::decode', function(){	
	it('should correctly decode input into RGB values (respectively)', function(){

	});
	it('should correctly decode input into RGB values (singular)', function(){

	});
	it('should correctly decode input into RGB values (hex)', function(){

	});
	it('should correctly decode input into RGB values (hex with alpha)', function(){

	});
	it('should correctly decode input into RGB values (text)', function(){

	});
	it('should correctly decode input into RGB values (object)', function(){

	});
});